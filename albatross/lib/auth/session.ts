/**
 * Auth boundary (placeholder).
 * Future: Supabase Auth session, Clerk, or native biometric session store.
 */
export type Session = null;

export async function getSession(): Promise<Session> {
  return null;
}
