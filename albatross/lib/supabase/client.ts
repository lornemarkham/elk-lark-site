/**
 * Supabase client (placeholder).
 * Future: `createClient` from `@supabase/supabase-js` with secure storage adapter.
 */
export function createSupabaseClient() {
  throw new Error('Supabase client not configured yet.');
}
